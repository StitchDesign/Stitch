What’s new:
- Project icons on home screen use a thumbnail of the project’s prototype window
- Node UI redesign

New layer node inputs:
- Padding + spacing
- “Stroke” now available on layers

Bug fixes + other changes:
- Fixed crash on first-run experience
- Fixed issue where 3D Model layer node wouldn't update transform
- Fixed various crashes caused by some input edits
- Fixed project deletion undo
- Graph edges use “curve” style by default
- Node type options in dropdown list in alphabetical order
- Re-enabled Cubic Bezier Animation patch node
- Text field layers with full screen prototype preview are now focusable
- Fixed issue where press interaction node may not work on first project load
- Node menu scroll position resets when reopened
- Improved default size of field for Text Field layer
- Added default value for SF Symbol node
